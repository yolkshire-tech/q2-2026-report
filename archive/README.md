# Archive

Superseded artifacts kept for historical reference — nothing in here feeds the live platform.

- **q2-2026-legacy-report/** — the original standalone Q2 2026 dashboard (7-tab HTML), its Python analysis (`Data/analyze.py`, hardcoded to an old machine's paths), the derived JSONs it produced, and `Data/cleaned_transactions.csv` (the invoice-level reference used in `Docs/Revenue_Baseline_Reconciliation.md`). Superseded by the app in `src/` + `pipeline/build_data.py`.
- **create_master_excel.py** — generator of the master workbook below; same stale paths.
- **Yolkshire_Master_Data_Q2_2026.xlsx** — a one-off export deliverable ("analysis with BI tools"), not a platform input.
